name: Agent scale refactor

on:
  push:
    branches:
      - agent/scale-denominator-refactor

permissions:
  contents: write

jobs:
  validate-runner:
    runs-on: ubuntu-latest
    steps:
      - name: Validate runner
        run: echo "Agent workflow is running"
